name: dart

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  cider: ^0.1.5
  flutter_gen: ^5.3.1
  flutterfire_cli: ^0.2.7
  grinder: ^0.9.4
  melos: ^3.0.1

dependency_overrides:
  cli_util: ^0.4.0
